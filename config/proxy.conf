server {
    listen 80;
    server_name project_domain;

    location / {
        include proxy_params;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_pass http://project_name_web:8888;
    }
}